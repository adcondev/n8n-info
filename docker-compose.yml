services:
  #################################################################
  # SERVICIO 1: LA CAJA FUERTE (Base de Datos PostgreSQL)
  # Aquí es donde vivirá toda la información de manera segura.
  #################################################################
  postgres:
    image: postgres:17-alpine  # Usamos la versión 'alpine' porque es muy ligera (ocupa poco espacio).
    restart: unless-stopped    # Si falla, intenta revivir automáticamente.
    
    # Credenciales para crear la caja fuerte (vienen del archivo .env)
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    
    # 💾 PERSISTENCIA: Esto es lo que evita que pase lo de Render.
    # Mapeamos una carpeta virtual (volume) para que los datos sobrevivan al reinicio.
    volumes:
      - postgres_data:/var/lib/postgresql/data
    
    # Red privada: Solo n8n necesita hablar con la base de datos, nadie más.
    networks:
      - n8n-internal
    
    # 🏥 SALUD: Verificación automática.
    # Dokploy preguntará: "¿Estás lista base de datos?" antes de encender n8n.
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      start_period: 30s
      interval: 10s
      timeout: 5s
      retries: 5

  #################################################################
  # SERVICIO 2: EL CEREBRO (n8n)
  # La aplicación donde ella crea sus flujos.
  #################################################################
  n8n:
    image: n8nio/n8n:latest
    restart: unless-stopped
    
    environment:
      # --- CONEXIÓN A LA BASE DE DATOS ---
      # Le decimos a n8n: "No uses SQLite, usa la caja fuerte de arriba"
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_HOST=postgres    # Nombre del servicio de arriba
      - DB_POSTGRESDB_PORT=5432
      - DB_POSTGRESDB_DATABASE=${POSTGRES_DB}
      - DB_POSTGRESDB_USER=${POSTGRES_USER}
      - DB_POSTGRESDB_PASSWORD=${POSTGRES_PASSWORD}
      
      # --- SEGURIDAD (CRÍTICO) ---
      # La llave para desencriptar las credenciales guardadas.
      - N8N_ENCRYPTION_KEY=${N8N_ENCRYPTION_KEY}
      
      # --- CONFIGURACIÓN GENERAL ---
      - N8N_HOST=${N8N_HOST}
      - N8N_PORT=5678
      - N8N_PROTOCOL=https
      - NODE_ENV=production
      - WEBHOOK_URL=https://${N8N_HOST}/
      - GENERIC_TIMEZONE=${GENERIC_TIMEZONE} # Importante para que los Cron (horarios) funcionen bien en Mazatlán.
      - N8N_SECURE_COOKIE=false # A veces necesario si SSL lo maneja el proxy.
      
    # Guardamos archivos locales de n8n (no la DB, sino caché y configuraciones menores)
    volumes:
      - n8n_data:/home/node/.n8n
      
    # Redes:
    # 1. n8n-internal: Para hablar con Postgres en secreto.
    # 2. dokploy-network: Para salir a internet a través de Dokploy.
    networks:
      - n8n-internal
      - dokploy-network
    
    # 🚦 ORDEN DE ARRANQUE:
    # "No inicies n8n hasta que Postgres esté saludable".
    depends_on:
      postgres:
        condition: service_healthy
        
    # 🌐 TRAEFIK (El Tránsito):
    # Estas etiquetas le dicen a Dokploy cómo enrutar el dominio "n8n.adcon.dev"
    labels:
      - traefik.enable=true
      - traefik.http.routers.n8n.rule=Host(`n8n.adcon.dev`)
      - traefik.http.routers.n8n.entrypoints=websecure
      - traefik.http.routers.n8n.tls=true
      - traefik.http.routers.n8n.tls.certresolver=letsencrypt
      - traefik.http.services.n8n.loadbalancer.server.port=5678
      - traefik.docker.network=dokploy-network

#################################################################
# DEFINICIÓN DE INFRAESTRUCTURA
#################################################################
networks:
  n8n-internal:
    driver: bridge   # Red privada interna
  dokploy-network:
    external: true   # Red pública de Dokploy (ya debe existir)

volumes:
  n8n_data:       # Espacio en disco para n8n
  postgres_data:  # Espacio en disco para la DB (¡Lo más importante!)
